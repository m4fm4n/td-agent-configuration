<source>
  @type http
  port 8888
</source>
 
<filter http.log>
  @type grep
  <regexp>
    key source
    pattern /localhost/
  </regexp>
</filter>

<match http.log>
  @type null
</match>

<match **>
  @type file
  path /var/log/fluent/access
</match>
